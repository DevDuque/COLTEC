<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Light Theme -->
    <style name="Theme.COLTEC.Light" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Use colors from colors.xml -->
        <item name="android:windowBackground">@color/backgroundColorLight</item>
    </style>

    <!-- Dark Theme -->
    <style name="Theme.COLTEC.Dark" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Use colors from colors.xml -->
        <item name="android:windowBackground">@color/backgroundColorDark</item>
    </style>
</resources>
